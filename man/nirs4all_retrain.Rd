% Generated by roxygen2: do not edit by hand
\name{nirs4all_retrain}
\alias{nirs4all_retrain}
\title{Retrain a fitted pipeline on fresh data}
\usage{
nirs4all_retrain(object, X, y = NULL)
}
\arguments{
\item{object}{A fitted \code{nirs4all_fit()} result.}

\item{X}{Numeric samples-by-features matrix or a \code{nirs4all_from_formats()} dataset.}

\item{y}{Target vector, or \code{NULL} when \code{X} is a nirs4all dataset.}
}
\value{
A newly fitted pipeline with independent state.
}
\description{
Reuses only the recipe (preprocessing steps and learner configuration), not
any fitted preprocessing state or model weights. The ordered input feature
schema must match the original fit. This also accepts a pipeline imported
with \code{nirs4all_import_trained_pipeline()}.
}
